//combining users
function combineUsers(...args){
  let combinedObject = {users: []}
};

//adding dates
require("datejs")






module.exports = {
  ...(typeof combineUsers !== 'undefined' && { combineUsers })
};